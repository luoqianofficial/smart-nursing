<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smartnursing.mapper.SysRoleMapper">

    <resultMap id="BaseResultMap" type="com.smartnursing.entity.SysRole">
            <id property="id" column="id" />
            <result property="roleName" column="role_name" />
            <result property="roleCode" column="role_code" />
            <result property="remark" column="remark" />
    </resultMap>

    <sql id="Base_Column_List">
        id,role_name,role_code,remark
    </sql>

    <select id="listRolesByUserId" resultMap="BaseResultMap">
        SELECT r.id, r.role_name, r.role_code, r.remark
        FROM sys_role r
        INNER JOIN sys_user_role ur ON ur.role_id = r.id
        WHERE ur.user_id = #{userId}
    </select>
</mapper>
