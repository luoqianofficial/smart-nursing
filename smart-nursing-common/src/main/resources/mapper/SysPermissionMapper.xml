<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smartnursing.mapper.SysPermissionMapper">

    <resultMap id="BaseResultMap" type="com.smartnursing.entity.SysPermission">
            <id property="id" column="id" />
            <result property="permName" column="perm_name" />
            <result property="permCode" column="perm_code" />
            <result property="permType" column="perm_type" />
    </resultMap>

    <select id="listPermCodeByUserId" resultType="String" >
        SELECT DISTINCT p.perm_code
        FROM sys_user_role ur
                 JOIN sys_role_perm rp ON ur.role_id = rp.role_id
                 JOIN sys_permission p ON rp.perm_id = p.id
        WHERE ur.user_id = #{userId}
    </select>

    <sql id="Base_Column_List">
        id,perm_name,perm_code,perm_type
    </sql>
</mapper>
